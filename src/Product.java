/**
 The Product class represents a product of two functions. It extends the Function class and provides methods for
 calculating the value, obtaining a string representation, and computing the derivative of the product function.
 */
public class Product extends MultiProduct {
    /**
     * Constructor for the Product class. product in this case is between 2 objects.
     *
     * @param firstFunction  from the Type of function
     * @param secondFunction from the type of function
     */
    public Product(Function firstFunction, Function secondFunction) {
        super(firstFunction, secondFunction);
    }
}


